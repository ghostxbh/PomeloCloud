var service = require('./network.service');

describe('network.test.js', function() {
  describe('network service', function() {
    it('should edit network', function(done) {
      expect(service).toBeDefined();
    });
  });
});
